#include <iostream>

int main ()
{
    // Inserir tamanho do Array
    int n;
    std::cin >> n;

    int array[n];

    // Definir elementos do Array
    for (int i = 0; i < n; i++)
    {
        std::cin >> sequencia[i];
    }

    

    
}